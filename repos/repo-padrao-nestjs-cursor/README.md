# repo-padrao-nestjs-cursor

**IDE:** Cursor · **Linguagem:** TypeScript (Node.js)

Template **NestJS API** — arquivos só para Cursor.

## Clone

```bash
git clone https://github.com/filipefalcaofs/repo-padrao-nestjs-cursor.git meu-app
cd meu-app
rm -rf .git && git init
npx @nestjs/cli new . --package-manager npm
```

Ver [`docs/stacks/NESTJS.md`](docs/stacks/NESTJS.md).

## Entrada da IDE

`.cursor/rules/`, `.cursor/skills/`, `.cursor/plugins/superpowers/`
