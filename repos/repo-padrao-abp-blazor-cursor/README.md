# repo-padrao-abp-blazor-cursor

**IDE:** Cursor · **Linguagem:** .NET (C#) · **UI:** blazor

Template **ABP + blazor** — arquivos só para Cursor.

## Clone

```bash
git clone https://github.com/filipefalcaofs/repo-padrao-abp-blazor-cursor.git meu-app
cd meu-app
rm -rf .git && git init
abp new MeuApp -u blazor -d ef
```

Ver [`docs/stacks/ABP.md`](docs/stacks/ABP.md).

Inclui `.mcp.json` — habilitar `abp-studio` no Cursor e abrir a solution no ABP Studio.

## Entrada da IDE

`.cursor/rules/`, `.cursor/skills/`, `.cursor/plugins/superpowers/`
