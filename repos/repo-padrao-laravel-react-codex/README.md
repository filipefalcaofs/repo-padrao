# repo-padrao-laravel-react-codex

**IDE:** Codex · **Linguagem:** PHP · **Frontend:** Inertia react

Template **Laravel + Inertia (react)** — arquivos só para Codex.

## Clone

```bash
git clone https://github.com/filipefalcaofs/repo-padrao-laravel-react-codex.git meu-app
cd meu-app
rm -rf .git && git init
composer create-project laravel/laravel .
composer require laravel/boost --dev
php artisan boost:install
```

Ver [`docs/stacks/LARAVEL.md`](docs/stacks/LARAVEL.md).

## Entrada da IDE

`AGENTS.md`, `.codex/skills/`
