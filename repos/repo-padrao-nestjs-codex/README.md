# repo-padrao-nestjs-codex

**IDE:** Codex · **Linguagem:** TypeScript (Node.js)

Template **NestJS API** — arquivos só para Codex.

## Clone

```bash
git clone https://github.com/filipefalcaofs/repo-padrao-nestjs-codex.git meu-app
cd meu-app
rm -rf .git && git init
npx @nestjs/cli new . --package-manager npm
```

Ver [`docs/stacks/NESTJS.md`](docs/stacks/NESTJS.md).

## Entrada da IDE

`AGENTS.md`, `.codex/skills/`
