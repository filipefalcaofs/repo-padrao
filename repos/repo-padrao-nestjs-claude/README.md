# repo-padrao-nestjs-claude

**IDE:** Claude Code · **Linguagem:** TypeScript (Node.js)

Template **NestJS API** — arquivos só para Claude Code.

## Clone

```bash
git clone https://github.com/filipefalcaofs/repo-padrao-nestjs-claude.git meu-app
cd meu-app
rm -rf .git && git init
npx @nestjs/cli new . --package-manager npm
```

Ver [`docs/stacks/NESTJS.md`](docs/stacks/NESTJS.md).

## Entrada da IDE

`CLAUDE.md`, `.claude/skills/`
